<%# Placeholder: the attention band (presenter.attention_pools).
    Task 6 fills this in; `presenter` is a HomePresenter. Deliberately renders
    nothing so Task 5 ships the route without pretending the screen is done. %>
