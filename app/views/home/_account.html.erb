<%# Placeholder: one account band (buffer, its pools, orphan pools).
    Task 7 fills this in; locals are `presenter` (HomePresenter) and `account`
    (a Pool of type account). Deliberately renders nothing for now. %>
