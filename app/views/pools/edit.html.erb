<%# Edit Savings Pool Page using standardized page header %>
<%= page_header(
    title: "Edit Savings Pool",
    subtitle: "Update details for #{@savings_pool.name}",
    breadcrumbs: [
      { label: "Savings Pools", url: savings_pools_path },
      { label: @savings_pool.name, url: savings_pool_path(@savings_pool) },
      { label: "Edit" }
    ]
) %>

<div class="max-w-2xl mx-auto">
  <%= render "form", savings_pool: @savings_pool %>
</div>
