<div class="min-h-screen flex items-center justify-center bg-gradient-to-br from-gray-50 via-white to-gray-100 p-6">
  <div class="w-full max-w-md">
    <!-- Logo/Brand -->
    <div class="text-center mb-12">
      <h2 class="text-4xl font-bold tracking-tight text-gray-900"><%= title %></h2>
      <p class="mt-3 text-base text-brand-dark"><%= subtitle %></p>
    </div>

    <!-- Card Container -->
    <div class="bg-white shadow-lg rounded p-10">
      <%= yield %>
    </div>

    <!-- Footer Links -->
    <div class="mt-8 text-center">
      <div>
        <%= footer %>
      </div>
    </div>
  </div>
</div> 