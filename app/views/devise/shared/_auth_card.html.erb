<div class="min-h-screen flex items-center justify-center bg-gradient-to-br from-[#f8f9fa] via-[#ffffff] to-[#e9ecef] p-6">
  <div class="w-full max-w-md">
    <!-- Logo/Brand -->
    <div class="text-center mb-12">
      <h2 class="text-4xl font-bold tracking-tight text-[#2d3436]"><%= title %></h2>
      <p class="mt-3 text-base text-[#5F741D]"><%= subtitle %></p>
    </div>

    <!-- Card Container -->
    <div class="bg-white shadow-lg rounded-2xl p-10">
      <%= yield %>
    </div>

    <!-- Footer Links -->
    <div class="mt-8 text-center">
      <div>
        <%= footer %>
      </div>
    </div>
  </div>
</div> 