<%= render layout: "devise/shared/auth_card",
    locals: {
      title: "Resend unlock instructions",
      subtitle: "We'll email you a link to unlock your account",
      footer: render("devise/shared/links")
    } do %>
  <%= form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post, class: "space-y-6" }) do |f| %>
    <%= render "devise/shared/error_messages", resource: resource %>

    <div>
      <%= f.label :email, class: "form-label" %>
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-input" %>
    </div>

    <%= f.submit "Resend unlock instructions", class: "w-full btn btn-primary-light" %>
  <% end %>
<% end %>
